#!/bin/bash
SP="$( cd "$(dirname "$0")" >/dev/null 2>&1 ; pwd -P )"
cd $SP
docker-compose -f $SP/docker-compose.yml up -d
rm ../logs/discord.js.log&&touch ../logs/discord.js.log
docker logs -f discordjs_bot_1 &> ../logs/discord.js.log &